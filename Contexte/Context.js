import React from 'react';

//Permet de stocker les données de token et username sur tous les écrans
export const TokenContext = React.createContext();
export const UsernameContext = React.createContext();
